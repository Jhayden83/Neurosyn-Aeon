- name: Init + Seal + Build + Audit
  run: |
    python codex/aeon_cli.py --aeon VAULT_BRIDGE.json init
    python codex/aeon_cli.py --aeon VAULT_BRIDGE.json seal "CI: build on $GITHUB_SHA"
    python codex/aeon_cli.py --aeon VAULT_BRIDGE.json build
    python codex/aeon_cli.py --aeon VAULT_BRIDGE.json audit
